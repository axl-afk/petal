# assets/

Empty on purpose — no bundled artwork or app icon was included with this
delivery. This folder is already wired up in `pubspec.yaml`
(`flutter: assets: - assets/`), so anything you drop in here (an app icon,
placeholder album art, fonts) will be picked up automatically on the next
`flutter pub get`.
